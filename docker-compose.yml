services:
  api:
    image: omnitenant-account:local
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    env_file:
      - .env
    ports:
      - "${API_PORT:-4000}:4000"
    # environment:
    #   NODE_ENV: production
    #   PORT: "4000"
    #   DATABASE_URL: postgres://postgres:postgres@db:5432/omnitenant
    #   JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-dev-access-secret-change-me-32chars}
    #   JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-dev-refresh-secret-change-me-32chars}
    #   ACCESS_TOKEN_TTL: ${ACCESS_TOKEN_TTL:-900}
    #   REFRESH_TOKEN_TTL_DAYS: ${REFRESH_TOKEN_TTL_DAYS:-30}
    #   PASSWORD_MIN_LENGTH: ${PASSWORD_MIN_LENGTH:-8}
    #   APP_BASE_URL: ${APP_BASE_URL:-http://localhost:3000}
    #   CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:3000}
    #   AUTH_RATE_LIMIT_MAX: ${AUTH_RATE_LIMIT_MAX:-100}
    #   AUTH_RATE_LIMIT_WINDOW_MS: ${AUTH_RATE_LIMIT_WINDOW_MS:-60000}
    # depends_on:
    #   db:
    #     condition: service_healthy
    restart: unless-stopped

  # db:
  #   image: postgres:16-alpine
  #   ports:
  #     - "${POSTGRES_PORT:-5432}:5432"
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: omnitenant
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d omnitenant"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

volumes:
  postgres_data:
